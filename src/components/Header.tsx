import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-white px-6 py-4 border-b border-gray-50 sticky top-0 z-50 min-h-[64px] flex items-center">
            <div className="max-w-md mx-auto w-full">
                {/* Empty header for minimalist look as requested */}
            </div>
        </header>
    );
};

export default Header;